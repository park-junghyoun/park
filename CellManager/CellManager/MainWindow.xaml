<Window x:Class="CellManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mahapps="http://mahapps.com/winfx/xaml/themes"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:CellManager.ViewModels"
        xmlns:viewsCellLibary="clr-namespace:CellManager.Views.CellLibary"
        xmlns:viewsTestSetup="clr-namespace:CellManager.Views.TestSetup"
        xmlns:views="clr-namespace:CellManager.Views"
        Title="Cell Manager" Height="800" Width="1400">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Home Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CellLibraryViewModel}">
            <viewsCellLibary:CellLibraryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:TestSetupViewModel}">
            <viewsTestSetup:TestSetupView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleViewModel}">
            <views:ScheduleBuilderView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RunViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Run Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DisplayViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Display Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AnalysisViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Analysis Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DataExportViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Data Export Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Settings Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HelpViewModel}">
            <Grid Background="#FFFFFF">
                <TextBlock Text="Help Content" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
            </Grid>
        </DataTemplate>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="25,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" BorderThickness="0" BorderBrush="Black" CornerRadius="5,0,0,5">
                            <ContentPresenter x:Name="ContentSite"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              ContentSource="Header"
                                              Margin="0,0,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#6799FF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888888"/>
                                <Setter TargetName="Border" Property="Background" Value="#DEDEDE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Margin="20,10">
                <materialDesign:PackIcon Kind="DeveloperBoard" />
                <TextBlock Text="{Binding BoardStatus}" VerticalAlignment="Center" Margin="0,0,50,0"/>

                <materialDesign:PackIcon Kind="Battery" />
                <TextBlock Text="{Binding SelectedCell.DisplayNameAndId}" VerticalAlignment="Center" Foreground="#007ACC" FontWeight="Bold"/>
            </StackPanel>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TabControl Grid.Column="0" TabStripPlacement="Left" 
                        BorderThickness="0" Background="#FAFAFA"
                        ItemsSource="{Binding NavigationItems,FallbackValue=sample}" 
                        SelectedItem="{Binding CurrentViewModel}" Grid.ColumnSpan="3">

                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Height="50" Width="200" Margin="0,0,0,0"  VerticalAlignment="Top" Background="#FFFFFF" Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="{Binding IconName, FallbackValue=Sample}" Height="30" Width="30" VerticalAlignment="Center" Margin="20,0,10,0"/>
                            <TextBlock Text="{Binding HeaderText, FallbackValue=Sample}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                        <Setter Property="IsEnabled" Value="{Binding IsViewEnabled}"/>
                    </Style>
                </TabControl.ItemContainerStyle>
            </TabControl>
        </Grid>
    </Grid>
</Window>