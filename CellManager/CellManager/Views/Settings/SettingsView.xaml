<UserControl x:Class="CellManager.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer>
        <StackPanel Margin="16">
            <!-- Board configuration -->
            <materialDesign:Card Margin="0,0,0,16" Padding="16">
                <StackPanel>
                    <TextBlock Text="Board Settings"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,16"/>
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Board Port"
                             Text="{Binding BoardPort}"
                             Width="200"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Spacing="8">
                        <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Calib X"
                                 Text="{Binding CalibrationX}"
                                 Width="120"/>
                        <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Calib Y"
                                 Text="{Binding CalibrationY}"
                                 Width="120"/>
                        <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Calib Z"
                                 Text="{Binding CalibrationZ}"
                                 Width="120"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>

            <!-- Board setting data -->
            <materialDesign:Card Margin="0,0,0,16" Padding="16">
                <StackPanel>
                    <TextBlock Text="Board Settings Data"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,16"/>
                    <DataGrid ItemsSource="{Binding BoardSettingsData}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              Margin="0,0,0,8">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Parameter" Binding="{Binding Parameter}"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding Value}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </materialDesign:Card>

            <!-- Path configuration -->
            <materialDesign:Card Margin="0,0,0,16" Padding="16">
                <StackPanel>
                    <TextBlock Text="Paths"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,16"/>
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Data Export Path"
                             Text="{Binding DataExportPath}"
                             Margin="0,0,0,8"
                             HorizontalAlignment="Stretch"/>
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Profile Path"
                             Text="{Binding ProfilePath}"
                             HorizontalAlignment="Stretch"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Feature switches -->
            <materialDesign:Card Margin="0,0,0,16" Padding="16">
                <StackPanel>
                    <TextBlock Text="Features"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,16"/>
                    <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                  Content="Enable Advanced Features"
                                  IsChecked="{Binding EnableAdvancedFeatures}"
                                  Margin="0,0,0,8"/>
                    <ToggleButton Style="{StaticResource MaterialDesignSwitchToggleButton}"
                                  Content="Enable Dark Theme"
                                  IsChecked="{Binding EnableDarkTheme}"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
